# Cord Language Instructions

## Symbols

`#` Means Constant number.

## Instructions

* `PUSH` - Pushes an 8bit number to a stack.
* `ADD` - Pops last 2 numbers off the stack and pushes result.
* `HLT` - Pops a stack and returns result as exit code.